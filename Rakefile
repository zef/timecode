# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/timecode.rb'

Hoe.new('timecode', Timecode::VERSION) do |p|
  # p.rubyforge_name = 'timecodex' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
